$('#editCategoryModal').modal('hide');
$('body').removeClass('modal-open');
$('.modal-backdrop').remove();
$('#navigation').html("<%= escape_javascript(render partial: 'pages/navigation', locals: {:@categories => @categories}) %>");